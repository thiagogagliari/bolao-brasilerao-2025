import React from 'react';

const ExampleComponent = () => {
    return (
        <div>
            <h2>Bem-vindo ao Bolão de Futebol!</h2>
            <p>Este é um componente de exemplo que pode ser utilizado para modularizar partes da interface do usuário.</p>
        </div>
    );
};

export default ExampleComponent;